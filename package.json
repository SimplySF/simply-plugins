{
  "name": "site",
  "private": true,
  "description": "Documentation site for the @simplysf/simply monorepo",
  "type": "module",
  "scripts": {
    "sync": "node ./scripts/sync-command-reference.mjs",
    "predev": "pnpm run sync",
    "dev": "astro dev",
    "prebuild": "pnpm run sync",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.9",
    "astro": "^7.2.2",
    "sharp": "^0.35.3"
  }
}
